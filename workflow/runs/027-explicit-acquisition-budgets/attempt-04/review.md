PASS

All acceptance criteria are materially satisfied. Active APIs/docs use separate observation and distinct-event budgets, duplicate-heavy behavior remains bounded, reports and presentation distinguish the required counts, authored-note limits use distinct IDs, and validation passes.