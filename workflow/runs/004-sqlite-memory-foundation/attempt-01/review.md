PASS

All acceptance criteria are materially satisfied. The public library and CLI share real SQLite storage, validate canonical events, deduplicate evidence while retaining observations, support reset and reproducible fixtures, expose schema-independent counts, close resources, and produce useful non-zero CLI failures. The reference-client sources match their pre-move versions.