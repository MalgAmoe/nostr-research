BLOCKED

The previous substantive discovery finding remains for hydration-report handles. `discoverResearchOperations()` recommends `pick`, `relate`, `remember-membership`, and `move` for `{ kind: "events", resultKind: "hydration-report" }`, while `descriptorCollection()` explicitly rejects hydration reports. Thus contextual discovery still advertises operations that preflight will reject.

Because this is the same invalid-operation discovery defect after another worker attempt, the workflow requires reassessment rather than a third mechanical correction.