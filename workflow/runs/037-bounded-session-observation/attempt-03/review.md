PASS

The bounded facet projection now uses the shared presentation module, preserves category counts and omitted counts, and has public console regression coverage. Relevant functional tests and syntax validation pass.