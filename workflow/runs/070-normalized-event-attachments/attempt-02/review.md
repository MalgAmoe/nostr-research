PASS

The previous dedicated-kind provenance issue is resolved. Supported content URLs in picture/video events now retain both declared dedicated-kind intent and separate URL-inference provenance. The public functional scenario verifies this path, and all acceptance criteria are materially satisfied by the implementation and successful validation output.