PASS

All acceptance criteria are materially satisfied. The previous catalog-boundary finding is fixed: synchronized state preserves `preview`, `count`, and `omitted`, with a suitable public functional scenario. Protected research-engine sources and tests remain unchanged, and validation passes.