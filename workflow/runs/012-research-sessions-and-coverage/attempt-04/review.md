PASS

All acceptance criteria are materially satisfied. The public run initialization defect is fixed for both `recordRun()` and `getRun()` results, with functional coverage preserving subjects, reasons, provenance, and run context. Repository validation passes: 19 tests, syntax checks, and `git diff --check`.