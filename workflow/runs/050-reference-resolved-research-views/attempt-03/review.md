PASS

All acceptance criteria are materially satisfied. Recursive truncation now propagates through nested arrays and objects, aggregate `by`/`min`/`max` metadata is truthful, and the public functional scenario verifies oversized nested tags before and after buffer turnover. Validation passes all 22 functional tests and syntax checks.