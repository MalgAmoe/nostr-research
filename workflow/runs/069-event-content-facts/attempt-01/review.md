PASS

All acceptance criteria are materially satisfied. The implementation matches the exact sparse kind mapping, reuses the exported relationship interpreter, preserves lazy evidence resolution, exposes the fields through relation/schema paths, and passes syntax, functional, and browser smoke validation.